import 'package:flutter/material.dart';

/// Returns a widget that displays a centered circular progress indicator.
Widget buildLoading() {
  return const Center(child: CircularProgressIndicator());
}
